import React from "react";
import { styled } from "@mui/system";

const Wrapper = styled("div")({
  flexGrow: 1,
  backgroundColor: "#36393f",
  marginTop: "48px",
  display: "flex",
});

const Messanger = () => {
  return <Wrapper>Messanger</Wrapper>;
};

export default Messanger;
